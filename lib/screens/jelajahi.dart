import 'package:flutter/material.dart';

class JelajahiScreen extends StatelessWidget {
  const JelajahiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Ini Jelajahi',
        style: TextStyle(
          color: Colors.white,
          fontSize: 30,
        ),
      ),
    );
  }
}
